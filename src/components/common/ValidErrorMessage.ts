import { styled } from "styled-components";

const ValidErrorMessage = styled.p`
  margin-top: 30px;
  font-size: var(--font-semi-small);
  text-align: center;
  color: var(--color-red);
`;

export default ValidErrorMessage;
